package com.bookService.core.test.cart.mongo.document;

/*
import jakarta.persistence.Id;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.springframework.data.mongodb.core.index.CompoundIndex;
import org.springframework.data.mongodb.core.mapping.Document;

import java.time.Instant;
import java.util.HashMap;
import java.util.Map;
*/

/*
@Document(collection = "carts1")
@CompoundIndex(name = "uid_idx", def = "{'userId': 1}")
@Data
@NoArgsConstructor
@AllArgsConstructor
*/
public class CartModelMapDocument1 {
/*
    @Id
    private String id;
    private String userId;
    // key: ISBN, value: { quantity: N }
    private Map<String, ItemEntry1> items = new HashMap<>();
    private Instant updatedAt;
*/
}

// 인덱스 적용